(ns cascalog.in-memory.tap)

;; Represent taps as in memory sequences


;; Deal with sinks - return sequences of tuples?

;; Deal with traps - collect tuples and somehow make them available for inspection at the end of the flow?

;; create some textline shim so textline coerces textlines into tuple generators
